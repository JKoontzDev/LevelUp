window.addEventListener('load', function() {
        const mapWrapper = document.querySelector('.map-wrapper');
        const areas = document.querySelectorAll('.area');
        areas.forEach(area => area.style.opacity = 1);
    });